import { Button } from './styles';
import { BackIcon } from '../../assets/icons/BackIcon'

const BackButton = () => {
  return (
    <>
      <Button>
        <BackIcon />
        Voltar
      </Button>
    </>
  );
};

export default BackButton;
